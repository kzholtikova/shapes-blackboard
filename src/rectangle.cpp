#include "../include/rectangle.h"

Rectangle::Rectangle(const Point& rectangleCenter, int rectangleWidth, int rectangleLength) {
    center = rectangleCenter;
    width = rectangleLength;
    length = rectangleLength;
}

void Rectangle::draw() {

}
